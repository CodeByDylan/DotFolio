# About

I build backend systems in C# and .NET — APIs, libraries and the tooling around them.

Most of what is here is open source. Each project below is assembled from its own repository, so
what you read is whatever that repository says about itself today.
